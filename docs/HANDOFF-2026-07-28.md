# HANDOFF — Kissaten POS rebuild (2026-07-28)

Đọc file này trước khi làm tiếp. Nhánh: **`launch-hardening`** (CHƯA merge vào main).

## 1. Đã làm gì (session này)

Ba plan lớn + nhiều tính năng, tất cả TDD + review 2 tầng + browser-verified. Specs/plans/ledger:
- `docs/superpowers/specs/2026-07-26-internal-order-gas-free-design.md`
- `docs/superpowers/specs/2026-07-26-internal-order-ui-rebuild-design.md`
- `docs/superpowers/specs/2026-07-28-pos-enhancements-design.md`
- Plans tương ứng trong `docs/superpowers/plans/`
- **Ledger đầy đủ (mọi task, fix, finding):** `.superpowers/sdd/progress.md` ← nguồn sự thật chi tiết.

**A. Backend GAS-free ban ngày** (print-server/): bảng `orders` materialized (SQLite WAL, optimistic version lock), `bill_engine.py` (sửa món/recompute total + **cancel ticket bếp modifier-aware** + **split atomic** chống nhân đôi tiền + merge), `online_inbox.py` (poll GAS mailbox, catch-up), `eod_sync.py`. Routes đọc/sửa/tách/gộp/void đơn.
  - **Quyết định kiến trúc:** giữ **syncer realtime proven** làm đường ghi GAS duy nhất (async ~3ms, resilient); read model local = UI hết đọc GAS ban ngày. **eod_sync SHELVED** (plist đã gỡ) — mutually-exclusive với syncer, fragile nếu bật.

**B. UI iPOS kds.html:** đọc đơn local + short-poll; **màn checkout** (sửa/tách/gộp/ghi chú/void PIN/in+thu); **hộp thư online** + badge cloud/máy in; **sơ đồ bàn 20 bàn**; **màn thu ngân**; nút **Xong hết đơn đã giao**; **Tem lẻ** (in tem món ngoài menu).

**C. Trang điện thoại `web/order.html`:** chọn bàn → menu → giỏ → gửi; **bộ chọn size/đá/đường/topping** (giá L đúng, topping cộng tiền); **lưới thu gọn + viết tắt** (TS/CF/CACAO/MH... — chỉ hiển thị, đơn/bill/tem dùng tên đầy đủ; dict `ABBR` sửa được trong order.html).

**D. Phiếu pha chế:** modifier + ghi chú **in TO gấp đôi + đậm** cho bếp (prep only); bill giữ thường.

**E. no-cache header:** app html/js/css luôn revalidate → tablet/điện thoại tự lấy bản mới.

## 2. ⚠️ CẦN DEPLOY (chưa có tác dụng tới khi restart)

Prod print_server đang chạy code cũ hơn vài commit backend. **Restart để nạp:** Part D (phiếu pha chế to), no-cache header, endpoint `/print/custom_label` (Tem lẻ), và mọi thay đổi print_server.py.
```bash
launchctl kickstart -k gui/$(id -u)/com.lamha.kissaten.printserver
```
- Restart **giữ token, không mất đơn**. Làm lúc vắng khách.
- HTML/JS (kds.html/order.html) serve fresh từ đĩa → chỉ cần reload; nhưng điện thoại có thể kẹt **cache cũ** → mở tab ẩn danh hoặc `?v=<số mới>` 1 lần (sau đó no-cache lo).
- URL: kds `http://192.168.50.125:5001/kds.html` · điện thoại `http://192.168.50.125:5001/order.html` (IP LAN Mac Mini; bind 0.0.0.0:5001).

## 3. Còn treo / quyết định cho session sau

- **B (đơn web) CHƯA làm:** cần phía Google — GAS action `pending_online_orders` + mitsu.cafe post đơn vào GAS mailbox. Giao diện hộp thư đã sẵn, hiện trống tới khi có phần này. (Đợt riêng, đụng GAS + trang khách.)
- **VOIDED chưa lên Sheets:** GAS `VALID_STATUS` không có VOIDED + không có void-reversal (void đơn đã PAID không hoàn revenue/tem trên Sheets). Audit local có (`void_reason`/`voided_by`). Cần task GAS riêng nếu muốn chủ soi void trên Sheets + hoàn tiền/tem.
- **order.html modifier chooser: XONG.** Nếu muốn tắt kiểu khác: sửa dict `ABBR` trong order.html.
- **Minor (non-blocking, ledger có chi tiết):** merge_bill/set_bill_group thiếu rollback (giống split bug đã fix, chưa vá); order.html không esc() (menu là nguồn tin cậy); sugar chip Tem lẻ hiện '50' thay vì '50%'.
- **Merge:** nhánh chưa vào main (248 commit ahead). Quyết định merge là của chủ.

## 4. Dev/test setup (QUAN TRỌNG — máy này chạy prod thật)

- **Prod LIVE port 5001** trên chính Mac Mini này, có **đơn thật** (~200+). TUYỆT ĐỐI không xoá `print-server/outbox.db` orders, không chạy test đụng máy in thật.
- **Test backend riêng:** port 5002, temp DB, noop (không in thật):
  ```bash
  cd print-server && PRINT_ENGINE=noop SERVER_PORT=5002 GATEWAY_DB=/tmp/kds_test_store.db \
    GAS_WEBAPP_URL="" GATEWAY_SYNC=0 ONLINE_POLL=0 python3 print_server.py &
  ```
  Verify browser: `http://localhost:5002/kds.html` hoặc `/order.html`. serve_kds/serve_order đọc file fresh → sửa xong reload.
- **Test:** pytest KHÔNG cài → `python3 -m unittest <mods> -v`. Safe suite (không hardware): `test_order_store test_bill_engine test_online_inbox test_eod_sync test_routes_orderstore` + `test_printlib`. JS: `node --test web/*.test.js`. **KHÔNG chạy** `test_routes.py`/`test_order_spool_e2e.py`/`app.run`/`RUN_EOD` (in thật trên POS live).
- `RECEIPT_FORMAT` mặc định **text** (prep dùng build_receipt_text, không raster). Prod=spool engine.

## 5. Kiến trúc nhanh

- `print-server/print_server.py` (Flask, ~2000 dòng): routes + serve kds/order/js. `GATEWAY`(outbox+syncer) · `STORE`(OrderStore) · `SPOOL` · `INBOX` — tất cả share 1 sqlite conn + lock.
- `order_store.py` bảng orders · `bill_engine.py` sửa/split/merge · `printlib.py` build_receipt(text/raster)+labels · `online_inbox.py` · `eod_sync.py`(dormant).
- `web/kds.html` (màn nội bộ, ~big) · `web/order.html` (điện thoại) · `web/order-api.js`+`checkout.js` (module dùng chung, node:test).
- Guardrail cũ (append-only Sheets, no external DB) vẫn đúng ở tầng GAS; local SQLite là scratch ban ngày.
